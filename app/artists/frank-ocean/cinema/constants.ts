// Shared cinema topology constants. This module is intentionally NOT a
// client module so server components (page shells, harnesses) can import
// the IDs without pulling a client boundary.

export const OVERLAY_ROOT_ID = 'cinema-overlay-root';
export const CINEMA_ROOT_ATTR = 'data-cinema-root';
